mod inputter;

struct Game {
    // if statement.command.eq("exit") || statement.command.eq("quit") {
// println!("game end");
// game_over(&mut state);
// }
}

fn main() {
    let mut inputter = inputter::Inputter::build();
    inputter.run();
}
